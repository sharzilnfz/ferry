# Golden fixtures are compared byte-for-byte against generated output;
# force LF everywhere so windows checkouts (autocrlf) cannot drift them.
crates/ferry-cli/tests/expected/*.txt text eol=lf

# Shell scripts must stay LF or bash chokes on CRLF.
*.sh text eol=lf
