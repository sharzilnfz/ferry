# 06: Refine Status Beacon Pulse Physicality

**What to build:**
Refine `@keyframes pulseBeacon` to maintain resting geometry (`scale(0.92)` ➔ `scale(1.3)`) with a gentle opacity decay, replacing previous aggressive implosion (`scale(0.6)`).

**Depends on:** None
**Blocks:** None

**Status:** closed

- [x] Refine scale and opacity boundaries of `@keyframes pulseBeacon` in `prototypes/fluid-glass/styles.css`.
- [x] Smooth beacon ring border-color and core background-color transitions.
